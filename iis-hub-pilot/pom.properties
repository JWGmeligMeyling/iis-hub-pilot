#================================================================================
# TEST SERVER
#================================================================================
project.test.server.scheme.http=http
project.test.server.scheme.https=https
project.test.server.host=localhost
project.test.server.port.http=18080
project.test.server.port.https=18443
project.test.server.context=/${project.artifactId}

#================================================================================
# TEST SERVER KEYSTORE
#================================================================================
project.test.server.keystore.file=${project.basedir}/../../iis-hub-pilot-crypto/iis-hub-pilot/iis-hub-pilot_test.p12
project.test.server.keystore.pass=pass
project.test.server.keystore.prov=BC
project.test.server.keystore.type=PKCS12

#================================================================================
# TEST SERVER TRUSTSTORE
#================================================================================
project.test.server.truststore.file=${project.basedir}/../../iis-hub-pilot-crypto/iis-hub-pilot/iis-hub-pilot_trust_test.p12
project.test.server.truststore.pass=pass
project.test.server.truststore.prov=BC
project.test.server.truststore.type=PKCS12

#================================================================================
# TEST SERVER SERVICE REQUESTS
#================================================================================
project.test.server.service.req.timeout=5000

#================================================================================
# TEST SERVER HUB SERVICE
#================================================================================
project.test.server.service.hub.url=${project.test.server.scheme.https}://${project.test.server.host}:${project.test.server.port.https}${project.test.server.context}/IISHubService
project.test.server.service.hub.url.wsdl=${project.test.server.service.hub.url}?wsdl
